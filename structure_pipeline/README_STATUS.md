# Directory Status

- category: `support`
- runtime_allowed: `false`
- read_only: `false`
- replacement_or_active_source: `当前 active baseline 不依赖此目录；结构相关主线目前以 SaProt-1.3B_emb 为准`

说明：

这里是结构检索 / 回收 / 下载链路的支撑目录。
它可以继续维护，但不是当前 baseline 的硬依赖。
